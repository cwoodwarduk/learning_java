public class Hello {
    //Param types must be declared
    public static void main(String[] args) {
         System.out.println("Hello, Calum");

         //Declaration Statement is used to define a variable
         //by indicating the data type, then name and optionally
         //set a value
         int myFirstNumber = (10 + 5) + (2 * 10);

         System.out.println(myFirstNumber);
    }
}
